/** A small sample document so the Reader can be exercised without a file. */
export const SAMPLE_MARKDOWN = `# The Reading Aid Tool

A short sample so you can see the **Reader** working before loading your own
book. The text below is *plain Markdown* — headings, paragraphs, a list, and a
quote — all normalized into the internal model.

## Why this exists

Reading slowly while mentally pronouncing every word consumes attention that
should go to comprehension. This tool nudges the eye forward so meaning lands
without sounding each word out.

It does that two ways:

- Bionic rendering anchors the eye on the shape of each word.
- A WPM pacer pulls you forward at a steady, chosen speed.

> The content is the product — the styling should stay out of the way.

That's the whole idea. Load a real Markdown file to read your own text.
`;
